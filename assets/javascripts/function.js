;(function ($) {
  "use strict";

  var body = $('body'),
      bodyHeight = body.height(),
      windowHeight = $(window).height(),
      windowWidth = $(window).width();

  var passItForm = {
    init: function () {
    },
  }

  $(document).ready(function () {
    passItForm.init();
  });
})(window.jQuery);
